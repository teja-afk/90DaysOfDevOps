---
server:
  name: prod-server
  ip: 192.168.0.1
  port: 8080

database:
  host: localhost
  name: app_db
  credentials:
    user: admin
    password: secret123

startup_script_pipe: |
  echo: "Starting server"
  docker compose up -d
  echo "Done"

startup_script_fold: >
  echo: "Starting server"
  docker compose up -d
  echo "Done"
